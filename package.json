{
  "name": "openclaw-token-audit",
  "version": "1.0.0",
  "description": "Token usage audit system for OpenClaw with anime-style dashboard",
  "scripts": {
    "dev": "cd dashboard && npm run dev",
    "build": "cd dashboard && npm run build",
    "collector": "cd collector && node index.js",
    "audit": "cd collector && node daily-audit.js"
  },
  "workspaces": ["collector", "dashboard"],
  "private": true
}